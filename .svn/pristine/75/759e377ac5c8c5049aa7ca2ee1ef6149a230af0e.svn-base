@extends('layouts.master')

@section('content')
<script type="text/javascipt">
	
</script>

<div class="row">
	<table class="span4 offset1 table">
		<tr>
			<td>Name</td>
			<td>{{$user->user_name}}</td>
		</tr>
		<tr>
			<td>Username</td>
			<td>{{$user->user_username}}</td>
		</tr>
		<tr>
			<td>Email</td>
			<td>{{$user->user_email}}</td>
		</tr>
		<tr>
			<td>Points</td>
			<td>{{$user->user_points}}</td>
		</tr>
	</table>
</div>
<a href="{{url('edit/profile')}}">Edit Profile</a>
<a href="{{url('edit/password')}}">Change Password</a>
<div class="row">
	<h4>Questions by me</h4>

	<table class="span4 offset1 table">
	<thead>
	<tr>
		<th>Question</th>
		<th>Answers</th>
		<th>Points</th>
	</tr>
	</thead> 

	<tbody>
	@foreach($questions as $question)
		<tr style='align:left'>
		<td><a href="{{url('view/question')}}?qid={{$question->post_id}}">{{ $question->type->question_title }}</a>
		<br>
		@foreach($question->type->tags as $tag)
			<a href="{{url('search/questions/tag')}}{{'/'.$tag->tag_name}}">{{$tag->tag_name." "}}</a>
		@endforeach
		</td>
		<td>{{$question->type->answers()->count('post_id')}} </td>
		<td> {{$question->type->question_points}} </td>
		</tr>
		@endforeach
	</tbody>
	</table>
</div>

<div class="row">
	<h4>Answers by me</h4>
	<table class="span4 offset1 table">
	<thead>
	<tr>
		<th>Answer</th>
		<th>Points</th>
	</tr>
	</thead> 

	<tbody>
	@foreach($answers as $answer)
	<tr style='align:left'>
		<td><a href="{{url('view/question')}}?qid={{$answer->type->question->post_id}}">{{ substr($answer->type->answer_body, 0, 50) }}...</a></td>
		
		<br>
		<td> {{$answer->type->answer_points}} </td>
		</tr>
		@endforeach
	</tbody>
	</table>

</div>

@stop