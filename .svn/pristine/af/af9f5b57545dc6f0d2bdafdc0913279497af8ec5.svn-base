@extends('layouts.master')

@section('content')

<div class="row">
	<div class="span4 offset1">
		@foreach($branches as $branch)
			<h3>{{$branch->branch_name}}</h3>
			<ul>
			@foreach($branch->subjects as $subject)
			<li><a href="{{url('univquestions/view')}}?sid={{$subject->subject_id}}">{{$subject->subject_name}}</a></li>
			@endforeach
			</ul>
		@endforeach
	</div>
</div>

@stop